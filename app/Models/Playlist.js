

export default class Playlist {
  constructor(id, highScore, maxScore, currentScore) {
    this.id = id;
    this.highScore = highScore;
    this.maxScore = maxScore;
    this.currentScore = currentScore;
  }

}